export default {
    topic: 'Compiler Design and Programming Languages',
    title: 'Compiler Design and Programming Languages',
    shortNamePrefix: 'CD',
    description: 'A comprehensive course on compiler construction and programming language theory, covering lexical analysis, parsing, abstract syntax trees, semantic analysis, type checking, intermediate representations, code generation, and optimization. Students learn to build compilers from front end to back end and understand the theoretical foundations of programming languages.',
    lectures: [
        { title: 'Introduction to Compilers and Language Processing', description: 'Overview of compiler structure, phases, and the compilation pipeline from source to machine code.', units: [
            { type: 'text', name: 'Compiler Phases Overview', content: 'A compiler translates source code through several phases: lexical analysis (scanning), syntax analysis (parsing), semantic analysis, intermediate code generation, optimization, and target code generation. Each phase transforms the program representation closer to machine code.' },
            { type: 'text', name: 'Interpreters vs Compilers', content: 'Compilers translate the entire program before execution, producing an executable. Interpreters execute the program statement by statement. JIT compilers combine both approaches by compiling hot code paths at runtime. Hybrid approaches like Java use bytecode compilation followed by JIT interpretation.' },
            { type: 'online', name: 'Compiler Explorer (Godbolt)', description: 'Interactive tool showing how different compilers transform source code into assembly language for various architectures and optimization levels.' },
            { type: 'text', name: 'Bootstrapping and Cross-Compilation', content: 'Bootstrapping is the process of writing a compiler in the language it compiles. T-diagrams visualize this process. Cross-compilers generate code for a different target platform. Self-hosting compilers are a milestone in language maturity.' },
            { type: 'attachment', name: 'Compiler Architecture Diagram' },
        ]},
        { title: 'Lexical Analysis and Regular Expressions', description: 'Scanning tokens from source code using finite automata and regular expressions.', units: [
            { type: 'text', name: 'Regular Expressions and Token Specification', content: 'Tokens are specified using regular expressions. Common token classes include identifiers ([a-zA-Z_][a-zA-Z0-9_]*), integers ([0-9]+), operators, and keywords. The longest match rule and priority rules resolve ambiguities when multiple patterns match.' },
            { type: 'text', name: 'Finite Automata for Scanning', content: 'NFAs are constructed from regular expressions using Thompson\'s construction. NFAs are converted to DFAs using the subset construction algorithm. DFAs are minimized using Hopcroft\'s algorithm. The resulting DFA drives the scanner with O(n) time complexity.' },
            { type: 'online', name: 'Regex to NFA/DFA Visualizer', description: 'Interactive tool that converts regular expressions to NFA and DFA diagrams, showing the subset construction and minimization steps.' },
            { type: 'text', name: 'Scanner Generators and Flex', content: 'Tools like Flex (Lex) generate scanners from regular expression specifications. The input is a set of pattern-action rules. Flex produces C code implementing a DFA-based scanner. Modern alternatives include ANTLR lexer rules and hand-written scanners for better error messages.' },
            { type: 'attachment', name: 'Lexical Analysis Exercises' },
        ]},
        { title: 'Context-Free Grammars and Parsing Theory', description: 'Formal grammars, derivations, parse trees, and the Chomsky hierarchy.', units: [
            { type: 'text', name: 'Context-Free Grammars', content: 'A CFG G = (V, T, P, S) consists of variables V, terminals T, productions P, and start symbol S. Derivations apply productions to expand variables. A language L(G) is the set of terminal strings derivable from S. CFGs define the syntax of programming languages.' },
            { type: 'text', name: 'Ambiguity and Grammar Transformations', content: 'A grammar is ambiguous if a string has multiple parse trees. The dangling else problem is a classic example. Techniques to resolve ambiguity include rewriting the grammar with precedence levels, using associativity rules, and adding disambiguation rules. Left recursion elimination and left factoring prepare grammars for top-down parsing.' },
            { type: 'online', name: 'Grammar Analysis Tool', description: 'Interactive tool for testing context-free grammars, computing FIRST and FOLLOW sets, and checking for ambiguity and LL(1) compatibility.' },
            { type: 'text', name: 'Chomsky Hierarchy', content: 'The Chomsky hierarchy classifies grammars into four types: Type 0 (unrestricted), Type 1 (context-sensitive), Type 2 (context-free), and Type 3 (regular). Programming language syntax is mostly context-free while semantics require context-sensitive analysis. Regular expressions are a subset of context-free grammars.' },
            { type: 'attachment', name: 'CFG Practice Problems' },
        ]},
        { title: 'Top-Down Parsing', description: 'Recursive descent, LL(1) parsing, FIRST and FOLLOW sets, and predictive parsers.', units: [
            { type: 'text', name: 'Recursive Descent Parsing', content: 'Recursive descent parsers implement one function per non-terminal. Each function examines the lookahead token to choose which production to apply. They are easy to write by hand and produce good error messages. Backtracking parsers try alternatives but are less efficient than predictive parsers.' },
            { type: 'text', name: 'LL(1) Parsing and Parse Tables', content: 'LL(1) parsers scan Left-to-right, produce Leftmost derivations, with 1 token lookahead. FIRST(A) is the set of terminals that can begin strings derived from A. FOLLOW(A) is the set of terminals that can appear after A. The LL(1) parse table maps (non-terminal, terminal) pairs to productions.' },
            { type: 'online', name: 'LL(1) Parse Table Generator', description: 'Online tool that computes FIRST and FOLLOW sets, constructs LL(1) parse tables, and simulates the parsing of input strings step by step.' },
            { type: 'text', name: 'Error Recovery in Top-Down Parsers', content: 'Panic mode recovery skips tokens until a synchronizing token is found. Phrase-level recovery replaces or inserts tokens to continue parsing. Error productions anticipate common mistakes. Burke-Fisher repair finds the minimum-cost repair within a window of tokens.' },
            { type: 'attachment', name: 'Top-Down Parsing Worksheet' },
        ]},
        { title: 'Bottom-Up Parsing', description: 'Shift-reduce parsing, LR(0), SLR, LALR, and LR(1) parsers.', units: [
            { type: 'text', name: 'Shift-Reduce Parsing', content: 'Bottom-up parsers build the parse tree from leaves to root by identifying handles (right-hand sides of productions) and reducing them. The parser uses a stack and performs shift (push token), reduce (pop handle, push non-terminal), accept, or error actions. Rightmost derivation is constructed in reverse.' },
            { type: 'text', name: 'LR Parsing Family', content: 'LR(0) items are productions with a dot marking the parsing position. SLR uses FOLLOW sets for reduction decisions. LALR merges LR(1) states with identical cores, used by yacc/bison. LR(1) items include a lookahead token for maximum power. LALR is the practical sweet spot between power and table size.' },
            { type: 'online', name: 'LR Parser Simulator', description: 'Interactive simulator for building LR(0), SLR, and LALR parse tables and tracing the shift-reduce parsing process on sample inputs.' },
            { type: 'text', name: 'Parser Generators: Yacc and Bison', content: 'Yacc (Yet Another Compiler Compiler) and GNU Bison generate LALR(1) parsers from grammar specifications with embedded semantic actions. Conflicts (shift-reduce, reduce-reduce) are resolved by precedence declarations or grammar restructuring. Modern alternatives include ANTLR (LL(*)) and tree-sitter.' },
            { type: 'attachment', name: 'Bottom-Up Parsing Exercises' },
        ]},
        { title: 'Abstract Syntax Trees and Intermediate Representations', description: 'AST construction, IR design, three-address code, and SSA form.', units: [
            { type: 'text', name: 'Abstract Syntax Trees', content: 'An AST is a simplified representation of the parse tree that omits syntactic sugar (parentheses, semicolons). Each node represents a language construct: expressions, statements, declarations. ASTs are the primary data structure for semantic analysis and are constructed during or after parsing using syntax-directed translation.' },
            { type: 'text', name: 'Three-Address Code and Quadruples', content: 'Three-address code (TAC) is an IR where each instruction has at most three operands: x = y op z. TAC linearizes the AST into a sequence of simple instructions. Quadruples (op, arg1, arg2, result) and triples (op, arg1, arg2) are storage formats. TAC simplifies optimization and code generation.' },
            { type: 'online', name: 'AST Visualization Tool', description: 'Interactive tool that parses source code and displays the resulting abstract syntax tree with node types, values, and parent-child relationships.' },
            { type: 'text', name: 'Static Single Assignment Form', content: 'SSA form ensures each variable is assigned exactly once. Phi functions merge values at control flow join points. SSA simplifies many optimizations: constant propagation, dead code elimination, and register allocation. LLVM IR is based on SSA form. Converting to SSA uses dominance frontiers.' },
            { type: 'attachment', name: 'IR Design Reference' },
        ]},
        { title: 'Semantic Analysis and Type Checking', description: 'Type systems, type inference, attribute grammars, and symbol tables.', units: [
            { type: 'text', name: 'Symbol Tables and Scope Resolution', content: 'Symbol tables map identifiers to their attributes (type, scope, memory location). Scoping rules determine identifier visibility: lexical (static) scoping uses the program structure, dynamic scoping uses the call stack. Nested scopes are implemented using a stack of hash tables or a single table with scope chains.' },
            { type: 'text', name: 'Type Systems and Type Checking', content: 'Type systems prevent type errors by assigning types to expressions and checking compatibility. Static typing checks types at compile time (Java, C++). Dynamic typing checks at runtime (Python, JavaScript). Strong typing prevents implicit coercions. Type rules are specified as inference rules in a type judgment system.' },
            { type: 'online', name: 'Type Inference Playground', description: 'Interactive environment for experimenting with Hindley-Milner type inference, showing unification steps and inferred types for functional programs.' },
            { type: 'text', name: 'Attribute Grammars and Syntax-Directed Translation', content: 'Attribute grammars associate attributes with grammar symbols and semantic rules with productions. Synthesized attributes flow bottom-up (e.g., expression types). Inherited attributes flow top-down (e.g., expected types). S-attributed grammars evaluate during bottom-up parsing; L-attributed during top-down.' },
            { type: 'attachment', name: 'Type Checking Exercises' },
        ]},
        { title: 'Runtime Environments and Memory Management', description: 'Stack frames, heap allocation, garbage collection, and calling conventions.', units: [
            { type: 'text', name: 'Stack Frames and Calling Conventions', content: 'Each function call creates an activation record (stack frame) containing parameters, local variables, return address, and saved registers. The frame pointer (FP) provides a stable base for accessing locals. Calling conventions (cdecl, stdcall, System V AMD64) define who saves registers and cleans the stack.' },
            { type: 'text', name: 'Heap Management and Allocation', content: 'Dynamic memory allocation uses heap memory managed by malloc/free or new/delete. Allocation strategies include first-fit, best-fit, and buddy systems. Memory fragmentation (internal and external) degrades performance. Memory pools and slab allocators optimize allocation for fixed-size objects.' },
            { type: 'online', name: 'Memory Layout Visualizer', description: 'Interactive visualization of program memory layout showing text, data, BSS, heap, and stack segments with dynamic allocation and deallocation.' },
            { type: 'text', name: 'Garbage Collection Algorithms', content: 'Garbage collection automatically reclaims unreachable memory. Mark-and-sweep marks reachable objects then frees unmarked ones. Copying collection divides memory into semispaces. Generational GC exploits the observation that most objects die young. Reference counting handles cycles with cycle detection algorithms.' },
            { type: 'attachment', name: 'Runtime Environment Worksheet' },
        ]},
        { title: 'Code Generation', description: 'Instruction selection, register allocation, and target code emission.', units: [
            { type: 'text', name: 'Instruction Selection', content: 'Instruction selection maps IR operations to target machine instructions. Tree pattern matching uses dynamic programming to find optimal instruction sequences covering the IR tree. CISC architectures offer complex instructions that can cover multiple IR nodes. Maximal munch is a greedy alternative.' },
            { type: 'text', name: 'Register Allocation', content: 'Register allocation assigns variables to a limited set of CPU registers. Graph coloring models this as coloring an interference graph with k colors (registers). Chaitin\'s algorithm builds the interference graph, simplifies nodes, and assigns colors. Spilling moves variables to memory when registers are insufficient.' },
            { type: 'online', name: 'Register Allocation Visualizer', description: 'Step-by-step visualization of graph coloring register allocation showing interference graph construction, simplification, and color assignment.' },
            { type: 'text', name: 'Code Generation from SSA', content: 'Generating code from SSA form requires eliminating phi functions by inserting copy instructions along incoming edges. Critical edge splitting ensures correct placement. Coalescing optimizes away unnecessary copies. Modern compilers like LLVM use SSA throughout the optimization pipeline.' },
            { type: 'attachment', name: 'Code Generation Problem Set' },
        ]},
        { title: 'Code Optimization', description: 'Local and global optimizations, data flow analysis, and loop optimizations.', units: [
            { type: 'text', name: 'Local and Peephole Optimizations', content: 'Local optimizations operate within basic blocks: common subexpression elimination, constant folding (evaluate constant expressions), constant propagation, algebraic simplification (x*1 -> x), and strength reduction (x*2 -> x<<1). Peephole optimization examines small windows of instructions for pattern-based improvements.' },
            { type: 'text', name: 'Data Flow Analysis', content: 'Data flow analysis computes information about program behavior at each point. Reaching definitions determine which assignments may reach a use. Live variable analysis identifies variables whose values may be used later. Available expressions enable common subexpression elimination. All are solved as fixed-point problems on CFG lattices.' },
            { type: 'online', name: 'Optimization Pass Explorer', description: 'Interactive tool showing how different compiler optimization passes transform code, with before/after comparisons of the IR at each stage.' },
            { type: 'text', name: 'Loop Optimizations', content: 'Loops dominate execution time so optimizations target them: loop-invariant code motion hoists computations outside loops, induction variable elimination replaces multiplications with additions, loop unrolling reduces branch overhead, and loop fusion/fission restructure loops for better cache behavior.' },
            { type: 'attachment', name: 'Optimization Techniques Summary' },
        ]},
        { title: 'LLVM and Modern Compiler Infrastructure', description: 'LLVM IR, pass infrastructure, and building compiler backends with LLVM.', units: [
            { type: 'text', name: 'LLVM IR and Its Design', content: 'LLVM IR is a typed, SSA-based intermediate representation. It supports three forms: in-memory (for passes), bitcode (binary on disk), and textual (.ll files). LLVM IR uses typed virtual registers, phi nodes, and a rich instruction set. It is target-independent, enabling reuse across architectures.' },
            { type: 'text', name: 'LLVM Pass Infrastructure', content: 'LLVM organizes optimizations as passes that transform or analyze IR. Module passes operate on entire translation units. Function passes work on individual functions. The pass manager handles dependencies and scheduling. Writing custom LLVM passes enables domain-specific optimizations.' },
            { type: 'online', name: 'LLVM Tutorial: Kaleidoscope', description: 'Official LLVM tutorial building a simple language compiler from scratch, covering lexing, parsing, AST construction, and LLVM IR generation.' },
            { type: 'text', name: 'Backend Code Generation in LLVM', content: 'LLVM\'s backend lowers IR to machine code through SelectionDAG instruction selection, register allocation, instruction scheduling, and assembly emission. TableGen descriptions define target machine instructions and registers. The MC layer handles object file emission for ELF, Mach-O, and COFF formats.' },
            { type: 'attachment', name: 'LLVM IR Quick Reference' },
        ]},
        { title: 'Advanced Topics: JIT Compilation and Language Runtimes', description: 'Just-in-time compilation, virtual machines, dynamic optimization, and language runtime design.', units: [
            { type: 'text', name: 'Just-In-Time Compilation', content: 'JIT compilers translate bytecode to native code at runtime. Method-based JIT compiles entire methods on first call. Tracing JIT records hot execution paths and compiles traces. Tiered compilation starts with interpretation, then baseline JIT, then optimizing JIT. The JVM HotSpot, V8, and LuaJIT are prominent examples.' },
            { type: 'text', name: 'Virtual Machines and Bytecode', content: 'Stack-based VMs (JVM, CPython) push/pop operands on a virtual stack. Register-based VMs (Lua, Dalvik) use virtual registers. Bytecode is a compact, portable IR. The JVM bytecode verifier ensures type safety before execution. WebAssembly is a modern bytecode format targeting browsers.' },
            { type: 'online', name: 'JVM Bytecode Viewer', description: 'Tool for inspecting compiled Java bytecode, showing the correspondence between source code and JVM instructions with stack frame details.' },
            { type: 'text', name: 'Dynamic Optimization and Deoptimization', content: 'Dynamic optimizers use profiling data to speculate on types and inline virtual methods. Speculative optimizations insert guards that check assumptions. When guards fail, the runtime deoptimizes: reconstructs the interpreter frame and falls back to slower code. This enables aggressive optimization of dynamic languages.' },
            { type: 'attachment', name: 'JIT Compilation Case Studies' },
        ]},
    ],
    exercises: {
        programming: [
            { title: 'Build a Lexical Scanner', problemStatement: 'Implement a lexical scanner for a simple programming language that recognizes identifiers, integer literals, floating-point literals, string literals, operators (+, -, *, /, =, ==, !=, <, >, <=, >=), and keywords (if, else, while, return, int, float). Use a DFA-based approach and handle whitespace and comments.' },
            { title: 'Regular Expression to NFA', problemStatement: 'Implement Thompson\'s construction algorithm to convert a regular expression into an NFA. Support concatenation, alternation (|), Kleene star (*), and parentheses. Represent the NFA as a graph and provide a method to simulate it on input strings.' },
            { title: 'NFA to DFA Conversion', problemStatement: 'Implement the subset construction algorithm to convert an NFA into an equivalent DFA. Compute epsilon closures and transition sets. Minimize the resulting DFA using Hopcroft\'s algorithm.' },
            { title: 'Recursive Descent Parser', problemStatement: 'Implement a recursive descent parser for arithmetic expressions with proper operator precedence and associativity. Support +, -, *, /, unary minus, parentheses, and integer/float literals. Build an AST from the parse and evaluate it.' },
            { title: 'LL(1) Parse Table Construction', problemStatement: 'Given a context-free grammar, compute FIRST and FOLLOW sets and construct an LL(1) parse table. Detect conflicts and report whether the grammar is LL(1). Implement a table-driven predictive parser using the generated table.' },
            { title: 'LR(0) Parser Generator', problemStatement: 'Implement an LR(0) parser generator that constructs the canonical collection of LR(0) items, builds the ACTION and GOTO tables, and detects shift-reduce and reduce-reduce conflicts. Simulate the shift-reduce parsing process on input token streams.' },
            { title: 'AST Construction and Pretty Printer', problemStatement: 'Define an AST node hierarchy for a small language supporting variable declarations, assignments, if-else statements, while loops, and arithmetic expressions. Implement a pretty printer that converts the AST back to formatted source code with proper indentation.' },
            { title: 'Symbol Table Implementation', problemStatement: 'Implement a symbol table supporting nested scopes using a stack of hash tables. Support operations for entering and exiting scopes, declaring variables with types, and looking up identifiers with proper scope resolution following lexical scoping rules.' },
            { title: 'Type Checker for a Simple Language', problemStatement: 'Implement a type checker that traverses an AST and verifies type correctness. Support integer, float, boolean, and string types with implicit widening (int to float). Report type errors with source locations for assignments, binary operations, function calls, and return statements.' },
            { title: 'Three-Address Code Generator', problemStatement: 'Implement a three-address code generator that translates an AST into a sequence of TAC instructions. Generate temporaries for intermediate values, handle short-circuit evaluation of boolean expressions, and emit labels and jumps for control flow statements.' },
            { title: 'Basic Block and CFG Builder', problemStatement: 'Given a sequence of three-address code instructions, identify basic blocks (leaders start after jumps and at jump targets) and construct the control flow graph. Implement methods to compute predecessors, successors, and dominators of each block.' },
            { title: 'Constant Folding and Propagation', problemStatement: 'Implement constant folding (evaluate constant expressions at compile time) and constant propagation (replace variables with known constant values) optimization passes on three-address code. Handle integer and floating-point arithmetic with proper overflow semantics.' },
            { title: 'Register Allocator via Graph Coloring', problemStatement: 'Implement a graph coloring register allocator. Build an interference graph from live variable analysis results, apply the simplify-select algorithm with k colors (registers), and handle spilling when the graph is not k-colorable. Output the register assignment for each variable.' },
            { title: 'Simple Interpreter with Environment', problemStatement: 'Build a tree-walking interpreter for a language with variables, arithmetic, comparisons, if-else, while loops, and print statements. Implement an environment model with lexical scoping. Support integer, float, boolean, and string values with dynamic type checking at runtime.' },
            { title: 'Garbage Collector: Mark and Sweep', problemStatement: 'Implement a mark-and-sweep garbage collector for a simple heap allocator. Maintain a root set of stack references, traverse the object graph to mark reachable objects, and sweep unreachable objects back to the free list. Track allocation statistics and support triggering collection on memory pressure.' },
        ],
        text: [
            { title: 'Compare LL and LR Parsing', problemStatement: 'Write a detailed comparison of LL and LR parsing techniques. Discuss their relative power (which grammars each can handle), ease of implementation, error recovery capabilities, and practical tool support. Provide specific examples of grammars that are LR(1) but not LL(1).' },
            { title: 'Static vs Dynamic Typing Essay', problemStatement: 'Analyze the trade-offs between static and dynamic type systems. Discuss type safety guarantees, developer productivity, runtime performance, and tooling support. Use examples from Java, Python, TypeScript, and Rust to illustrate your points.' },
            { title: 'Garbage Collection Strategies', problemStatement: 'Compare and contrast the major garbage collection algorithms: mark-and-sweep, copying collection, generational GC, and reference counting. Discuss throughput, pause times, memory overhead, and suitability for different application domains such as real-time systems and server applications.' },
            { title: 'LLVM Impact on Compiler Development', problemStatement: 'Discuss how LLVM has changed the landscape of compiler development. Cover its modular architecture, reusable optimization passes, multi-target support, and its role in enabling new languages like Rust and Swift. Contrast with the traditional monolithic compiler approach.' },
            { title: 'Optimization Trade-offs in Compilers', problemStatement: 'Explain the trade-offs compilers face during optimization. Discuss compilation time vs code quality, code size vs execution speed, and the phase ordering problem. Describe how optimization levels (-O0 through -O3, -Os) balance these trade-offs in GCC and LLVM.' },
        ],
        modeling: [
            { title: 'Compiler Architecture Class Diagram', problemStatement: 'Design a class diagram showing the main components of a compiler: Lexer, Parser, SemanticAnalyzer, IRGenerator, Optimizer, and CodeGenerator. Include the Token, AST node hierarchy, and Symbol Table classes with their relationships and key methods.', diagramType: 'ClassDiagram' },
            { title: 'Parsing Process Activity Diagram', problemStatement: 'Create an activity diagram showing the step-by-step process of LR parsing including reading input tokens, consulting the parse table, performing shift and reduce actions, and handling errors until the input is accepted or rejected.', diagramType: 'ActivityDiagram' },
            { title: 'AST Node Hierarchy', problemStatement: 'Design a class diagram for an AST node hierarchy including abstract base ASTNode, Expression (BinaryExpr, UnaryExpr, Literal, Identifier), Statement (IfStmt, WhileStmt, AssignStmt, ReturnStmt, BlockStmt), and Declaration (VarDecl, FuncDecl) classes with their fields.', diagramType: 'ClassDiagram' },
            { title: 'Garbage Collection Activity Diagram', problemStatement: 'Create an activity diagram for a generational garbage collector showing the steps of minor collection (young generation), promotion to old generation, and major collection with mark-sweep-compact phases.', diagramType: 'ActivityDiagram' },
            { title: 'Type System Class Diagram', problemStatement: 'Design a class diagram for a type system including Type (IntType, FloatType, BoolType, StringType, ArrayType, FunctionType, VoidType), TypeEnvironment, and TypeChecker classes showing inheritance and composition relationships.', diagramType: 'ClassDiagram' },
        ],
        quiz: [
            { title: 'Lexical Analysis and Parsing Quiz', questions: [
                { type: 'multiple-choice', title: 'Regular Expressions', text: 'Which formalism is used to specify the tokens of a programming language?', points: 5, singleChoice: true, answerOptions: [{ text: 'Regular expressions', isCorrect: true }, { text: 'Context-free grammars', isCorrect: false }, { text: 'Turing machines', isCorrect: false }, { text: 'Pushdown automata', isCorrect: false }] },
                { type: 'multiple-choice', title: 'Parser Power', text: 'Which parser type can handle the largest class of context-free grammars?', points: 5, singleChoice: true, answerOptions: [{ text: 'LR(1)', isCorrect: true }, { text: 'LL(1)', isCorrect: false }, { text: 'SLR', isCorrect: false }, { text: 'LR(0)', isCorrect: false }] },
            ]},
            { title: 'Semantic Analysis and Types Quiz', questions: [
                { type: 'multiple-choice', title: 'Type Checking', text: 'In which phase of compilation are type errors typically detected?', points: 5, singleChoice: true, answerOptions: [{ text: 'Semantic analysis', isCorrect: true }, { text: 'Lexical analysis', isCorrect: false }, { text: 'Code generation', isCorrect: false }, { text: 'Parsing', isCorrect: false }] },
                { type: 'multiple-choice', title: 'Symbol Tables', text: 'What data structure is commonly used to implement symbol tables with nested scopes?', points: 5, singleChoice: true, answerOptions: [{ text: 'Stack of hash tables', isCorrect: true }, { text: 'Binary search tree', isCorrect: false }, { text: 'Linked list', isCorrect: false }, { text: 'Array', isCorrect: false }] },
            ]},
            { title: 'Code Generation and Optimization Quiz', questions: [
                { type: 'multiple-choice', title: 'SSA Form', text: 'What is the key property of Static Single Assignment (SSA) form?', points: 5, singleChoice: true, answerOptions: [{ text: 'Each variable is assigned exactly once', isCorrect: true }, { text: 'Each variable is used exactly once', isCorrect: false }, { text: 'There are no branch instructions', isCorrect: false }, { text: 'All variables are stored in registers', isCorrect: false }] },
                { type: 'multiple-choice', title: 'Register Allocation', text: 'Which graph theory problem is register allocation typically modeled as?', points: 5, singleChoice: true, answerOptions: [{ text: 'Graph coloring', isCorrect: true }, { text: 'Maximum flow', isCorrect: false }, { text: 'Shortest path', isCorrect: false }, { text: 'Minimum spanning tree', isCorrect: false }] },
            ]},
        ],
        fileUpload: [
            { title: 'Compiler Front End Report', problemStatement: 'Upload a report documenting the design and implementation of a compiler front end for a small language. Include the grammar specification, scanner and parser design decisions, AST structure, and sample inputs with their corresponding AST visualizations.' },
            { title: 'Optimization Pass Analysis', problemStatement: 'Upload an analysis comparing the effect of different optimization passes on a set of benchmark programs. Measure execution time and code size with and without constant folding, dead code elimination, and loop-invariant code motion. Include performance charts.' },
        ],
    },
    faqs: [
        { title: 'What is the difference between a compiler and an interpreter?', answer: 'A compiler translates the entire source program into machine code or another target language before execution. An interpreter executes the source program statement by statement without producing a separate executable. JIT compilers are a hybrid: they compile code to native instructions at runtime. Compilers offer faster execution while interpreters provide faster development cycles.' },
        { title: 'Why do we need separate lexical and syntax analysis phases?', answer: 'Separating lexing and parsing simplifies the compiler design through modularity. The lexer handles character-level details (whitespace, comments, token boundaries) using efficient finite automata. The parser works with tokens at a higher abstraction level using context-free grammars. This separation also improves performance: DFA-based scanning is faster than parsing character by character.' },
        { title: 'What is the difference between an NFA and a DFA?', answer: 'An NFA (Nondeterministic Finite Automaton) can have multiple transitions from a state on the same input and epsilon transitions. A DFA (Deterministic Finite Automaton) has exactly one transition per input symbol per state. NFAs are easier to construct from regular expressions (Thompson\'s construction). DFAs are more efficient to simulate (O(n) time). The subset construction converts NFA to DFA.' },
        { title: 'What is the dangling else problem?', answer: 'The dangling else ambiguity occurs when an else clause could associate with either an inner or outer if statement: if (a) if (b) s1 else s2. Most languages resolve this by associating else with the nearest unmatched if. Solutions include rewriting the grammar to separate matched and unmatched if statements, or using disambiguation rules in the parser generator.' },
        { title: 'What are FIRST and FOLLOW sets used for?', answer: 'FIRST(A) is the set of terminals that can begin strings derived from non-terminal A. FOLLOW(A) is the set of terminals that can appear immediately after A in any sentential form. They are used to construct LL(1) parse tables: a production A -> alpha is placed in cell (A, t) if t is in FIRST(alpha), or if alpha can derive epsilon and t is in FOLLOW(A).' },
        { title: 'What is the difference between LL and LR parsing?', answer: 'LL parsers work top-down, building the parse tree from root to leaves, scanning Left-to-right and producing Leftmost derivations. LR parsers work bottom-up, building from leaves to root, producing Rightmost derivations in reverse. LR parsers handle a larger class of grammars including left-recursive ones. LL parsers are simpler to implement by hand (recursive descent).' },
        { title: 'What is an abstract syntax tree and how does it differ from a parse tree?', answer: 'A parse tree (concrete syntax tree) represents every grammar rule applied during parsing, including all tokens. An AST is a simplified version that omits syntactic details like parentheses, semicolons, and chain productions. For example, the expression (3 + 4) has a parse tree with nodes for the parentheses, but the AST just has an Add node with children 3 and 4.' },
        { title: 'What is three-address code?', answer: 'Three-address code (TAC) is an intermediate representation where each instruction has at most three operands: x = y op z. Complex expressions are broken into sequences of simple instructions using temporary variables. For example, a + b * c becomes t1 = b * c; t2 = a + t1. TAC is easy to optimize and translate to machine code.' },
        { title: 'What is Static Single Assignment (SSA) form?', answer: 'SSA form is an IR property where each variable is assigned exactly once. When a variable would be assigned multiple times (e.g., in different branches), a phi function merges the values at the join point. SSA simplifies optimizations like constant propagation, dead code elimination, and register allocation. LLVM IR is inherently in SSA form.' },
        { title: 'How does register allocation work?', answer: 'Register allocation assigns variables to CPU registers. It is modeled as graph coloring: nodes represent variables, edges connect variables that are live at the same time (interfere). Coloring the graph with k colors (where k is the number of available registers) gives a valid assignment. If the graph cannot be k-colored, some variables are spilled to memory.' },
        { title: 'What is constant folding vs constant propagation?', answer: 'Constant folding evaluates constant expressions at compile time (e.g., 3 + 4 becomes 7). Constant propagation replaces variable uses with their known constant values (if x = 5, then y = x + 1 becomes y = 5 + 1, which then folds to y = 6). Both are applied iteratively as one optimization enables the other.' },
        { title: 'What is loop-invariant code motion?', answer: 'Loop-invariant code motion (LICM) identifies computations inside a loop whose results do not change across iterations and hoists them outside the loop. For example, if x = a + b is inside a loop and a and b do not change, the computation is moved before the loop. This reduces redundant work proportional to the number of loop iterations.' },
        { title: 'What is the difference between static and dynamic typing?', answer: 'Static typing checks types at compile time, catching errors before execution (Java, C++, Rust). Dynamic typing checks types at runtime, allowing more flexibility but deferring errors (Python, JavaScript, Ruby). Static typing enables better optimization and tooling. Dynamic typing offers faster prototyping. Gradual typing (TypeScript) bridges both approaches.' },
        { title: 'How does garbage collection work?', answer: 'Garbage collection automatically reclaims memory that is no longer reachable from the program\'s root set (stack variables, globals). Mark-and-sweep traverses the object graph marking reachable objects, then frees unmarked ones. Generational GC divides objects by age, collecting young objects frequently. Trade-offs include throughput, pause times, and memory overhead.' },
        { title: 'What is a phi function in SSA?', answer: 'A phi function appears at control flow join points in SSA form to select the correct value based on which predecessor block executed. For example, after if-else branches that assign different values to x, a phi function x3 = phi(x1, x2) merges the values. Phi functions are not real instructions; they are resolved during code generation by inserting copies.' },
        { title: 'What is the role of the LLVM framework?', answer: 'LLVM provides reusable compiler infrastructure: a well-defined IR (LLVM IR), optimization passes, and target-independent code generation. Language developers only need to write a front end that emits LLVM IR. LLVM handles optimization and code generation for multiple architectures (x86, ARM, RISC-V). Languages like Rust, Swift, and Julia use LLVM as their backend.' },
        { title: 'What is a context-free grammar?', answer: 'A context-free grammar (CFG) is a formal grammar where every production rule has a single non-terminal on the left side: A -> alpha. The right side can be any mix of terminals and non-terminals. CFGs define the syntax of programming languages and are recognized by pushdown automata. They are more powerful than regular expressions but less powerful than context-sensitive grammars.' },
        { title: 'What is the difference between a scanner and a parser?', answer: 'A scanner (lexer) reads characters and groups them into tokens (identifiers, numbers, operators). It uses finite automata and operates in O(n) time. A parser reads tokens and builds a parse tree according to a context-free grammar. Scanners handle regular languages; parsers handle context-free languages. Together they form the compiler front end.' },
        { title: 'What is a JIT compiler?', answer: 'A Just-In-Time (JIT) compiler translates bytecode or IR to native machine code at runtime. It can use profiling data to optimize hot code paths aggressively. Examples include the JVM HotSpot JIT, V8 (JavaScript), and PyPy (Python). JIT compilers balance compilation overhead with execution speed, typically using tiered compilation from interpreter to optimized native code.' },
        { title: 'What are the main optimization levels in GCC and LLVM?', answer: '-O0 disables optimization for fast compilation and easy debugging. -O1 applies basic optimizations with moderate compile time. -O2 enables most optimizations for a good speed-size balance. -O3 enables aggressive optimizations including loop vectorization and function inlining. -Os optimizes for code size. -Ofast allows unsafe math optimizations. Each level enables specific pass sets.' },
    ],
    exerciseMessages: [
        'I am stuck on the lexer exercise. My scanner does not correctly handle multi-character operators like == and !=. How do I implement the longest match rule?',
        'Has anyone figured out how to handle nested comments in the scanner? My regex-based approach does not work for /* ... /* ... */ ... */ style nesting.',
        'My NFA to DFA conversion produces way too many states. Is there a way to minimize the DFA after construction?',
        'I think there is a bug in my FIRST set computation. It does not handle epsilon productions correctly. Should I add epsilon to FIRST(A) if all symbols on the right side can derive epsilon?',
        'The recursive descent parser keeps going into infinite recursion. I think I have left recursion in my grammar. How do I eliminate it?',
        'My LL(1) parse table has a conflict in the entry for (Expr, LPAREN). Does this mean my grammar is not LL(1)?',
        'I am confused about how to build the AST during parsing. Should I create nodes in the semantic actions of each grammar rule?',
        'The three-address code generator produces correct code but the temporaries are not being reused. Is it okay to generate unlimited temporaries?',
        'My type checker reports an error for implicit int-to-float conversion. Should I allow widening conversions automatically or require explicit casts?',
        'How do I handle the scope of variables declared inside a for loop? Should the variable be visible after the loop exits?',
        'I implemented the symbol table but looking up identifiers in deeply nested scopes is slow. Should I use a hash map chain instead of a linked list?',
        'The LR(0) parser generator shows a shift-reduce conflict for the if-else grammar. This is the dangling else problem, right? How do I resolve it?',
        'My constant folding pass crashes when dividing by zero at compile time. Should I emit an error or leave the division as a runtime operation?',
        'I passed all tests on the basic block construction exercise but my CFG is missing back edges for loops. How are back edges identified?',
        'Can someone explain how phi functions work? I do not understand when to insert them and how they get resolved to actual instructions.',
        'My register allocator spills everything when there are only 4 available registers. The interference graph seems correct. Is my simplification order wrong?',
        'The code generator exercise asks for x86 assembly but I have never written assembly before. Are there any good references for x86 instruction set?',
        'I am getting a segfault in my mark-and-sweep GC. I think the issue is that I am not scanning all root pointers on the stack correctly.',
        'Does the interpreter exercise require us to handle closures? The problem statement mentions functions but does not explicitly say first-class functions.',
        'My parser builds the correct AST for simple expressions but fails for chained comparisons like a < b < c. How should this be parsed?',
        'The optimization exercise says to implement dead code elimination. If a variable is assigned but never used, do I remove just the assignment or also the computation on the right-hand side?',
        'I am struggling with the short-circuit evaluation in the TAC generator. For a && b, I should not evaluate b if a is false, right? How do I generate the control flow for this?',
        'My lexer correctly tokenizes identifiers but does not distinguish keywords from identifiers. Should I use a keyword table lookup after matching an identifier pattern?',
        'The Thompson construction exercise requires supporting the plus (+) operator. Can I just convert r+ to rr* or should I handle it natively in the NFA?',
        'I have a question about the grammar for function declarations. Should the parameter list allow trailing commas or is that a syntax error?',
        'My SLR parser works for simple grammars but has conflicts on the full language grammar. Should I switch to LALR?',
        'The type inference exercise mentions Hindley-Milner. Do we need to implement full unification or just a simplified version?',
        'How do I handle string escapes (like \\n and \\t) in the lexer? Should I convert them to actual characters during scanning or keep them as two-character sequences?',
        'My live variable analysis computes different results depending on iteration order. Is the analysis supposed to be a backward flow problem?',
        'I am not sure how to handle function calls in the three-address code. Do I need to generate instructions for pushing arguments onto the stack?',
        'The register allocation exercise mentions coalescing. When is it safe to merge two nodes in the interference graph?',
        'My interpreter handles recursive functions but runs out of stack space for deep recursion. Should I implement tail call optimization?',
        'The pretty printer exercise asks for proper indentation but my output has inconsistent spacing in nested if-else blocks. How do I track the indentation level?',
        'Can we assume the input programs are syntactically correct for the type checker exercise, or do we also need to handle parse errors?',
        'My DFA minimization groups too many states together and the resulting DFA rejects valid inputs. I think my partition refinement is wrong.',
        'The control flow graph builder misses edges for break and continue statements inside loops. How do I handle these in the CFG construction?',
        'I finished the lexer exercise but it runs very slowly on large files. Should I be reading character by character or buffering the input?',
        'My AST evaluator gives wrong results for expressions with mixed integer and float operands. When exactly should I promote int to float?',
        'The quiz says LALR merges states with identical cores. What happens if merging introduces new conflicts that were not present in the canonical LR(1) table?',
        'I implemented the garbage collector but it collects objects that are still in use. I think my root scanning misses some references stored in global variables.',
        'My parser error recovery using panic mode skips too many tokens and misses subsequent errors. Is there a better synchronization strategy?',
        'How do I test the code generator? Should I compare my assembly output against a reference compiler or actually run it?',
        'The optimization pass ordering matters - constant propagation enables more constant folding. How many times should I iterate the passes?',
        'My three-address code for while loops generates a jump to the condition check at the bottom of the loop body. Is this the standard layout?',
        'I am confused about inherited vs synthesized attributes. The type of a variable declaration is inherited by the identifier node, right?',
        'My regex to NFA converter produces an NFA with epsilon transitions everywhere. Is there a way to reduce the number of epsilon transitions?',
        'The exercise says to implement reaching definitions analysis. Is this a forward or backward data flow analysis?',
        'Does anyone know if we need to handle multi-dimensional arrays in the type checker? The grammar supports them but the test cases only use 1D arrays.',
        'Just finished the full compiler pipeline exercise! The key was to test each phase independently before connecting them together.',
        'My generated code runs but produces wrong results for nested function calls like f(g(x)). I think my calling convention handling is incorrect.',
    ],
    channelMessages: [
        'Can someone explain why LALR(1) is used in practice instead of full LR(1)? Is it just about table size?',
        'I am confused about the difference between syntax-directed definitions and syntax-directed translation schemes. Are they the same thing?',
        'How do real compilers like GCC handle error recovery? Do they use panic mode or something more sophisticated?',
        'What is the relationship between regular expressions and finite automata? Can every regex be converted to a DFA?',
        'Why does LLVM use SSA form for its IR? What advantages does it provide over non-SSA representations?',
        'Can someone explain the concept of a basic block? Why are they important for optimization?',
        'How does the JVM verify bytecode before execution? What properties does the bytecode verifier check?',
        'What is the phase ordering problem in compiler optimization? Does the order in which passes run affect the final output?',
        'I do not understand why left recursion is a problem for top-down parsers but not for bottom-up parsers.',
        'How do modern garbage collectors achieve low pause times? What techniques does the G1 or ZGC collector use?',
        'Can someone walk through how the subset construction algorithm works with a concrete NFA example?',
        'What is the difference between tree pattern matching and macro expansion for instruction selection?',
        'How do compilers handle type coercion between integers and floating-point numbers during code generation?',
        'Why is Rust able to avoid garbage collection while still guaranteeing memory safety? How does the borrow checker work at the compiler level?',
        'What are the trade-offs between stack-based and register-based virtual machines for bytecode interpreters?',
    ],
};
