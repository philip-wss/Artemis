export default {
    topic: 'Embedded Systems and IoT',
    title: 'Embedded Systems and Internet of Things',
    shortNamePrefix: 'ES',
    description: 'This course covers embedded systems from microcontroller fundamentals to IoT applications. Topics include real-time operating systems, sensor interfacing, communication protocols (I2C, SPI, UART), interrupt handling, firmware development, power management, FPGA basics, digital signal processing, actuator control, wireless protocols (BLE, Zigbee), and embedded Linux.',
    lectures: [
        { title: 'Microcontroller Fundamentals', description: 'Architecture, registers, and memory organization of microcontrollers.', units: [
            { type: 'text', name: 'Microcontroller Architecture', content: 'A microcontroller integrates CPU, memory (Flash for program, SRAM for data), and peripherals on a single chip. Harvard architecture separates instruction and data buses for simultaneous fetch and access. ARM Cortex-M series dominates the 32-bit embedded market. Key components include ALU, register file, program counter, and stack pointer.' },
            { type: 'text', name: 'Memory Organization', content: 'Flash memory stores firmware (non-volatile, electrically erasable). SRAM provides fast volatile data storage. EEPROM stores configuration parameters that persist across power cycles. Memory-mapped I/O maps peripheral registers into the address space. The linker script defines memory regions: .text (code), .data (initialized variables), .bss (zero-initialized), and .stack.' },
            { type: 'online', name: 'ARM Cortex-M Architecture Explorer', description: 'Interactive tool for exploring the ARM Cortex-M processor architecture including registers, pipeline stages, and the memory map.' },
            { type: 'text', name: 'GPIO and Pin Configuration', content: 'General-Purpose Input/Output pins are the primary interface to external hardware. Each pin can be configured as input (floating, pull-up, pull-down) or output (push-pull, open-drain). Alternate function modes connect pins to peripherals (UART, SPI, I2C). Drive strength and slew rate affect signal integrity. Pin multiplexing allows flexible peripheral mapping.' },
            { type: 'attachment', name: 'Microcontroller Datasheet Exercises' },
        ]},
        { title: 'Interrupt Handling and Exceptions', description: 'Interrupt mechanisms, priority schemes, and exception handling in embedded systems.', units: [
            { type: 'text', name: 'Interrupt Mechanism', content: 'Interrupts allow peripherals to asynchronously signal the CPU. When an interrupt fires, the CPU saves context (registers, PC, status), jumps to the Interrupt Service Routine (ISR), processes the event, restores context, and returns. The Nested Vectored Interrupt Controller (NVIC) on ARM Cortex-M supports up to 240 interrupt sources with configurable priority levels.' },
            { type: 'text', name: 'Interrupt Priority and Nesting', content: 'Priority levels determine which interrupt preempts another. ARM Cortex-M supports priority grouping: preemption priority and sub-priority. Lower numerical priority values indicate higher urgency. Nested interrupts allow a higher-priority ISR to preempt a lower-priority one. Priority inversion occurs when a low-priority task holds a resource needed by a high-priority ISR.' },
            { type: 'online', name: 'NVIC Configuration Simulator', description: 'Simulator for configuring the Nested Vectored Interrupt Controller, setting priorities, enabling interrupts, and observing preemption behavior.' },
            { type: 'text', name: 'ISR Design Best Practices', content: 'ISRs must be short and deterministic to minimize interrupt latency. Avoid blocking operations, dynamic memory allocation, and printf in ISRs. Use flags or ring buffers to defer processing to the main loop or an RTOS task. Volatile keyword prevents compiler optimization of shared variables. Critical sections disable interrupts briefly to protect shared resources.' },
            { type: 'attachment', name: 'Interrupt Handling Lab Guide' },
        ]},
        { title: 'Serial Communication Protocols', description: 'UART, SPI, and I2C protocols for peripheral and sensor communication.', units: [
            { type: 'text', name: 'UART Protocol', content: 'UART is an asynchronous serial protocol using TX and RX lines. Data frames consist of a start bit, 5-9 data bits, optional parity bit, and 1-2 stop bits. Common baud rates: 9600, 115200. No clock signal; both sides must agree on the baud rate. Flow control uses RTS/CTS or XON/XOFF. UART is point-to-point and full-duplex.' },
            { type: 'text', name: 'SPI Protocol', content: 'SPI (Serial Peripheral Interface) uses four signals: SCLK (clock), MOSI (master out, slave in), MISO (master in, slave out), and CS (chip select, active low). Full-duplex, synchronous, master-slave topology. Clock polarity (CPOL) and phase (CPHA) define four SPI modes. SPI achieves high data rates (up to 100 MHz) but requires one CS line per slave.' },
            { type: 'online', name: 'Protocol Analyzer Tool', description: 'Virtual logic analyzer tool for decoding UART, SPI, and I2C waveforms with timing analysis and protocol violation detection.' },
            { type: 'text', name: 'I2C Protocol', content: 'I2C (Inter-Integrated Circuit) uses two bidirectional lines: SDA (data) and SCL (clock). Multi-master, multi-slave bus with 7-bit or 10-bit addressing. Standard mode runs at 100 kHz, fast mode at 400 kHz, fast mode plus at 1 MHz. Start condition, address byte, R/W bit, ACK/NACK, data bytes, stop condition. Pull-up resistors required on both lines.' },
            { type: 'attachment', name: 'Communication Protocol Comparison Sheet' },
        ]},
        { title: 'Real-Time Operating Systems', description: 'RTOS concepts, task scheduling, and synchronization mechanisms.', units: [
            { type: 'text', name: 'RTOS Fundamentals', content: 'An RTOS provides deterministic scheduling with guaranteed response times. Key concepts: tasks (threads), scheduler, tick interrupt, and kernel services. FreeRTOS is the most widely used open-source RTOS. Tasks have states: running, ready, blocked, suspended. The scheduler selects the highest-priority ready task. Context switching saves and restores CPU registers and stack pointers.' },
            { type: 'text', name: 'Task Scheduling Algorithms', content: 'Preemptive priority-based scheduling: highest priority task runs immediately. Rate Monotonic Scheduling (RMS) assigns priority inversely to period. Earliest Deadline First (EDF) is optimal for dynamic priority. Time slicing (round-robin) shares CPU among equal-priority tasks. Schedulability analysis determines if all deadlines can be met: utilization bound for RMS is n(2^(1/n) - 1).' },
            { type: 'online', name: 'FreeRTOS Task Scheduler Visualizer', description: 'Interactive visualizer showing task state transitions, context switches, and timeline execution for different scheduling policies in FreeRTOS.' },
            { type: 'text', name: 'Synchronization Primitives', content: 'Mutexes provide mutual exclusion with priority inheritance to prevent priority inversion. Semaphores signal between tasks (binary for synchronization, counting for resource pools). Queues pass data between tasks safely. Event groups allow tasks to wait on combinations of flags. Deadlock occurs when tasks circularly wait for each other\'s resources.' },
            { type: 'attachment', name: 'RTOS Configuration and Tuning Guide' },
        ]},
        { title: 'Sensor Interfacing and Data Acquisition', description: 'Analog-to-digital conversion, sensor types, and signal conditioning.', units: [
            { type: 'text', name: 'ADC Fundamentals', content: 'Analog-to-Digital Converters sample continuous signals into discrete digital values. Resolution (bits) determines granularity: 12-bit ADC with 3.3V reference gives 3.3/4096 = 0.8 mV per step. Sampling rate must satisfy Nyquist: f_sample >= 2 * f_max. SAR ADCs balance speed and resolution for most embedded applications. Sigma-delta ADCs provide high resolution at lower speeds.' },
            { type: 'text', name: 'Common Sensor Types', content: 'Temperature: thermistors (NTC/PTC), thermocouples, digital sensors (DS18B20, BME280). Inertial: accelerometers (MEMS capacitive), gyroscopes (MEMS Coriolis), magnetometers. Distance: ultrasonic (HC-SR04), infrared (Sharp GP2Y0A21), LiDAR (ToF). Light: photodiodes, phototransistors, ambient light sensors (BH1750). Each type requires specific interfacing and calibration.' },
            { type: 'online', name: 'Sensor Calibration Workshop', description: 'Hands-on workshop for calibrating common sensors including temperature compensation, linearity correction, and multi-point calibration procedures.' },
            { type: 'text', name: 'Signal Conditioning', content: 'Signal conditioning prepares sensor outputs for ADC input. Amplification scales small signals (instrumentation amplifiers for bridge sensors). Filtering removes noise: low-pass RC filters for anti-aliasing, active filters for precision. Level shifting matches sensor output range to ADC input range. Isolation (optocouplers, magnetic isolators) protects against ground loops and high voltages.' },
            { type: 'attachment', name: 'Sensor Interface Circuit Schematics' },
        ]},
        { title: 'Firmware Development Practices', description: 'Embedded C programming, hardware abstraction layers, and debugging techniques.', units: [
            { type: 'text', name: 'Embedded C Programming', content: 'Embedded C uses standard C with hardware-specific extensions. Volatile qualifier prevents compiler optimization of hardware registers and shared variables. Bitwise operations manipulate individual register bits: SET (|=), CLEAR (&= ~), TOGGLE (^=), CHECK (&). Packed structs map to hardware register layouts. Inline assembly accesses special CPU instructions (WFI, DSB, ISB).' },
            { type: 'text', name: 'Hardware Abstraction Layers', content: 'HAL provides a portable API over hardware registers. STM32 HAL, Nordic nRF SDK, and ESP-IDF are vendor-specific HALs. Benefits: portability across chip families, reduced development time. Drawbacks: abstraction overhead, less control than bare-metal register access. CMSIS (Cortex Microcontroller Software Interface Standard) standardizes core and peripheral access for ARM Cortex-M.' },
            { type: 'online', name: 'Firmware Debugging with GDB and OpenOCD', description: 'Tutorial on debugging firmware using GDB with OpenOCD, setting breakpoints, inspecting registers, and using SWD/JTAG interfaces for on-chip debugging.' },
            { type: 'text', name: 'Build Systems and Toolchains', content: 'Cross-compilation targets a different architecture than the host. ARM GCC toolchain (arm-none-eabi-gcc) compiles for ARM Cortex-M. CMake and Makefiles manage build configurations. Linker scripts define memory layout, entry point, and section placement. The startup code initializes the stack, copies .data from Flash to SRAM, zeros .bss, and calls main().' },
            { type: 'attachment', name: 'Firmware Project Template' },
        ]},
        { title: 'Power Management and Low-Power Design', description: 'Sleep modes, power optimization, and energy harvesting for battery-powered systems.', units: [
            { type: 'text', name: 'Power Consumption Fundamentals', content: 'Embedded power consumption has dynamic (switching) and static (leakage) components. Dynamic power: P = C * V^2 * f. Reducing clock frequency and supply voltage dramatically lowers power. Modern MCUs offer multiple power domains that can be independently controlled. Current measurement with shunt resistors or dedicated power analyzers (Nordic PPK, Otii Arc) profiles power consumption over time.' },
            { type: 'text', name: 'Sleep Modes and Wake Sources', content: 'ARM Cortex-M defines Sleep, Deep Sleep, and Standby modes with progressively lower power. Sleep mode halts the CPU but keeps peripherals running (microamps). Deep Sleep disables most clocks, retains SRAM (sub-microamp). Standby powers off everything except the RTC and backup registers (nanoamps). Wake sources: external interrupts, RTC alarms, watchdog timers, and UART activity detection.' },
            { type: 'online', name: 'Power Profiling Tool', description: 'Interactive tool for analyzing power consumption traces, identifying power-hungry code sections, and estimating battery life for different usage profiles.' },
            { type: 'text', name: 'Energy Harvesting', content: 'Energy harvesting captures ambient energy to power embedded devices. Solar cells with MPPT (Maximum Power Point Tracking) for outdoor IoT. Thermoelectric generators (TEG) exploit temperature differentials. Piezoelectric harvesters convert vibration to electricity. RF energy harvesting captures ambient radio energy. Supercapacitors and rechargeable batteries store harvested energy for burst operations.' },
            { type: 'attachment', name: 'Battery Life Estimation Worksheet' },
        ]},
        { title: 'FPGA Fundamentals', description: 'Programmable logic, HDL design, and FPGA architecture for embedded applications.', units: [
            { type: 'text', name: 'FPGA Architecture', content: 'FPGAs contain configurable logic blocks (CLBs) with lookup tables (LUTs) and flip-flops, connected by a programmable routing network. Modern FPGAs include DSP slices, block RAM, PLLs, and hard IP (PCIe, Ethernet MACs). Xilinx (AMD) and Intel (Altera) dominate the market. FPGAs excel at parallel processing, custom interfaces, and real-time signal processing where software is too slow.' },
            { type: 'text', name: 'VHDL and Verilog Basics', content: 'Hardware Description Languages describe digital circuits structurally or behaviorally. VHDL: strongly typed, verbose, common in Europe and defense. Verilog: C-like syntax, concise, common in the US. Combinational logic uses concurrent assignments. Sequential logic uses clocked processes (VHDL) or always blocks (Verilog). Simulation verifies functionality before synthesis maps HDL to hardware resources.' },
            { type: 'online', name: 'FPGA Development Environment Setup', description: 'Guide to setting up Vivado or Quartus development environments, creating projects, writing HDL, simulating designs, and programming FPGA boards.' },
            { type: 'text', name: 'FPGA in Embedded Systems', content: 'FPGAs serve as custom peripherals, protocol bridges, and hardware accelerators in embedded systems. Soft-core processors (MicroBlaze, Nios II, RISC-V) implement CPUs inside the FPGA fabric. SoC FPGAs (Xilinx Zynq, Intel Cyclone V SoC) combine ARM hard cores with FPGA fabric. AXI bus connects the processor to custom FPGA IP blocks.' },
            { type: 'attachment', name: 'FPGA Design Lab Exercises' },
        ]},
        { title: 'Digital Signal Processing for Embedded Systems', description: 'Filters, FFT, and real-time signal processing on resource-constrained platforms.', units: [
            { type: 'text', name: 'Digital Filters', content: 'FIR (Finite Impulse Response) filters have guaranteed stability and linear phase. Implemented as convolution: y[n] = sum(h[k] * x[n-k]). IIR (Infinite Impulse Response) filters achieve sharper cutoffs with fewer coefficients but can be unstable. Butterworth, Chebyshev, and Bessel are common IIR designs. Fixed-point arithmetic avoids floating-point overhead on MCUs without FPU.' },
            { type: 'text', name: 'FFT on Microcontrollers', content: 'The Fast Fourier Transform converts time-domain signals to frequency domain in O(N log N). ARM CMSIS-DSP library provides optimized FFT routines for Cortex-M. Radix-2 FFT requires power-of-2 buffer sizes. Windowing (Hanning, Hamming, Blackman) reduces spectral leakage. Real-time FFT uses ping-pong buffers: one fills via DMA while the other is processed.' },
            { type: 'online', name: 'DSP Algorithm Visualizer', description: 'Interactive visualizer for digital filters and FFT showing frequency response, pole-zero plots, and real-time signal processing with adjustable parameters.' },
            { type: 'text', name: 'CMSIS-DSP Library', content: 'CMSIS-DSP provides optimized math and DSP functions for ARM Cortex-M. Categories: basic math, fast math, complex math, filters (FIR, IIR, LMS), matrix operations, transforms (FFT, DCT), statistical functions, and interpolation. Functions use fixed-point (Q15, Q31) and floating-point formats. Hardware MAC (Multiply-Accumulate) instructions accelerate filter computation.' },
            { type: 'attachment', name: 'DSP Implementation Exercises' },
        ]},
        { title: 'Actuator Control and PWM', description: 'Motor control, servo driving, and PWM generation for actuators.', units: [
            { type: 'text', name: 'PWM Generation', content: 'Pulse Width Modulation controls average power by varying the duty cycle of a digital signal. Timer peripherals generate hardware PWM with configurable frequency and resolution. 16-bit timer at 72 MHz gives PWM frequency = 72MHz/65536 = 1.1 kHz. Complementary PWM with dead-time insertion drives H-bridge motor controllers. Center-aligned mode reduces switching noise.' },
            { type: 'text', name: 'Motor Control', content: 'DC motors: PWM controls speed, H-bridge controls direction. Brushless DC (BLDC) motors require electronic commutation with Hall sensors or back-EMF sensing. Stepper motors use step/direction signals for precise positioning. PID control maintains desired speed or position: error = setpoint - measured, output = Kp*e + Ki*integral(e) + Kd*derivative(e). Tuning Kp, Ki, Kd is critical.' },
            { type: 'online', name: 'PID Controller Tuning Simulator', description: 'Simulator for tuning PID controller parameters with different plant models, showing step response, overshoot, settling time, and steady-state error.' },
            { type: 'text', name: 'Servo and Actuator Interfacing', content: 'RC servos accept 50 Hz PWM with 1-2 ms pulse width mapping to 0-180 degrees. Industrial servos use digital protocols (RS-485, EtherCAT). Linear actuators extend and retract with position feedback via potentiometer or encoder. Solenoids provide binary on/off actuation. Flyback diodes protect against inductive kickback when driving inductive loads.' },
            { type: 'attachment', name: 'Motor Control Circuit Designs' },
        ]},
        { title: 'Wireless Protocols for IoT', description: 'BLE, Zigbee, LoRa, and Wi-Fi for wireless embedded communication.', units: [
            { type: 'text', name: 'Bluetooth Low Energy (BLE)', content: 'BLE operates in the 2.4 GHz ISM band with 40 channels (3 advertising, 37 data). GATT (Generic Attribute Profile) organizes data into services and characteristics. BLE 5.0 supports 2 Mbps PHY, extended advertising, and long range (coded PHY). Connection intervals trade latency for power: 7.5 ms to 4 s. Typical range is 10-100 m depending on TX power and environment.' },
            { type: 'text', name: 'Zigbee and Thread', content: 'Zigbee uses IEEE 802.15.4 at 2.4 GHz with mesh networking for home automation. Supports coordinator, router, and end device roles. Thread is an IPv6-based mesh protocol built on 802.15.4 for IoT. Matter (formerly Project CHIP) unifies smart home protocols over Thread and Wi-Fi. Zigbee 3.0 standardizes application-layer interoperability across vendors.' },
            { type: 'online', name: 'IoT Protocol Comparison Dashboard', description: 'Interactive dashboard comparing wireless protocols (BLE, Zigbee, LoRa, Wi-Fi, Thread) across range, data rate, power consumption, topology, and cost metrics.' },
            { type: 'text', name: 'LoRa and LPWAN', content: 'LoRa (Long Range) uses chirp spread spectrum modulation for ranges exceeding 10 km with sub-GHz frequencies. LoRaWAN defines the network architecture with gateways, network servers, and end devices. Spreading factors (SF7-SF12) trade data rate for range. Duty cycle limits (1% in EU) constrain uplink frequency. Ideal for low-data-rate sensor networks: agriculture, smart cities, asset tracking.' },
            { type: 'attachment', name: 'Wireless Protocol Selection Guide' },
        ]},
        { title: 'Embedded Linux and System-on-Chip', description: 'Linux kernel, device drivers, and buildroot/Yocto for embedded platforms.', units: [
            { type: 'text', name: 'Embedded Linux Overview', content: 'Embedded Linux runs on application processors (Cortex-A, RISC-V) with MMU support. The boot sequence: bootloader (U-Boot) loads kernel and device tree, kernel initializes hardware and mounts root filesystem, init system starts user-space services. Embedded Linux suits applications needing networking, filesystems, USB, and display — unlike bare-metal or RTOS for hard real-time.' },
            { type: 'text', name: 'Device Tree and Kernel Configuration', content: 'The Device Tree describes hardware topology in a structured format (.dts files). Properties define base addresses, interrupt numbers, clock frequencies, and pin configurations. The kernel parses the device tree at boot to discover and configure peripherals. Kconfig (make menuconfig) customizes kernel features, drivers, and modules. Cross-compilation targets ARM, RISC-V, or MIPS architectures.' },
            { type: 'online', name: 'Yocto Project Build Tutorial', description: 'Step-by-step tutorial for building a custom embedded Linux distribution using the Yocto Project with BitBake recipes, layers, and image customization.' },
            { type: 'text', name: 'Linux Device Drivers', content: 'Character device drivers expose peripheral access via /dev entries with open, read, write, ioctl operations. Platform drivers bind to device tree nodes. The kernel driver model uses probe/remove functions for device lifecycle. DMA engines handle high-throughput data transfers. Sysfs exports driver attributes to user space. User-space drivers via UIO or /dev/mem simplify prototyping but bypass kernel safety.' },
            { type: 'attachment', name: 'Embedded Linux Getting Started Guide' },
        ]},
    ],
    exercises: {
        programming: [
            { title: 'GPIO LED Toggle', problemStatement: 'Write bare-metal firmware that configures GPIO pins as outputs and toggles LEDs at different rates using hardware timer interrupts. Implement debounced button input to change the blink pattern.' },
            { title: 'UART Echo Server', problemStatement: 'Implement a UART driver from scratch using register-level programming. Configure baud rate, data format, and interrupts. Build an echo server that receives characters and sends them back, supporting ring buffer-based TX/RX with interrupt-driven I/O.' },
            { title: 'I2C Temperature Sensor Driver', problemStatement: 'Write a driver for a digital temperature sensor (e.g., TMP102) over I2C. Implement bus initialization, start/stop conditions, addressing, register reads/writes, and temperature conversion with 0.0625-degree resolution.' },
            { title: 'SPI Flash Memory Interface', problemStatement: 'Implement a driver for an SPI NOR flash memory (e.g., W25Q128). Support read, page program, sector erase, status register polling, and write protection. Handle the flash erase-before-write constraint correctly.' },
            { title: 'FreeRTOS Task Scheduler', problemStatement: 'Create a FreeRTOS application with multiple tasks at different priorities. Implement producer-consumer communication using queues, protect shared resources with mutexes, and demonstrate priority inversion with and without priority inheritance.' },
            { title: 'PWM Motor Controller', problemStatement: 'Implement a PWM-based DC motor speed controller with direction control via an H-bridge. Use timer capture to read encoder feedback and implement a PID control loop that maintains the motor at a target RPM.' },
            { title: 'ADC Data Logger', problemStatement: 'Build a multi-channel ADC data logger that samples analog sensors using DMA transfers, applies a moving average filter, converts raw values to engineering units, and stores timestamped readings to an SD card via SPI.' },
            { title: 'BLE Sensor Beacon', problemStatement: 'Develop a BLE peripheral that advertises sensor data (temperature, humidity, battery level) using a custom GATT service. Implement connection handling, characteristic notifications, and a low-power advertising schedule to maximize battery life.' },
            { title: 'Interrupt-Driven Keypad Scanner', problemStatement: 'Implement a matrix keypad scanner using row-column scanning with GPIO interrupts. Handle key debouncing with a timer-based state machine, support multi-key detection, and provide key press/release callbacks to the application.' },
            { title: 'Watchdog Timer Manager', problemStatement: 'Implement a software watchdog system that monitors multiple tasks. Each task registers with the watchdog and must periodically report alive status. If any task fails to check in, the system logs the fault, attempts recovery, and triggers a hardware reset as a last resort.' },
            { title: 'Bootloader Implementation', problemStatement: 'Write a minimal bootloader that receives firmware updates over UART using the XMODEM protocol. Implement CRC verification, flash programming of the application region, integrity checking on boot, and fallback to a known-good firmware image on failure.' },
            { title: 'DMA Memory Transfer Engine', problemStatement: 'Configure and use the DMA controller for memory-to-memory, memory-to-peripheral, and peripheral-to-memory transfers. Implement circular buffer mode for continuous ADC sampling and double-buffering for SPI display updates without CPU intervention.' },
            { title: 'Real-Time Clock Alarm System', problemStatement: 'Implement an alarm system using the RTC peripheral. Support setting multiple alarms with configurable recurrence (one-shot, daily, weekly). Use low-power modes between alarms, waking the system only when an alarm triggers. Persist alarm settings across power cycles using backup registers.' },
            { title: 'CAN Bus Communication', problemStatement: 'Implement a CAN 2.0B driver for inter-node communication. Configure bit timing for 500 kbps, set up hardware message filters, implement standard and extended frame transmission/reception, and handle bus-off recovery. Build a simple vehicle sensor data network.' },
            { title: 'Embedded Web Server', problemStatement: 'Build a lightweight HTTP server on an ESP32 or similar Wi-Fi-enabled MCU. Serve a web page displaying real-time sensor data via AJAX polling or WebSocket. Implement RESTful API endpoints for reading sensors and controlling actuators with JSON responses.' },
        ],
        text: [
            { title: 'RTOS vs Bare-Metal Trade-offs', problemStatement: 'Compare and contrast using an RTOS versus bare-metal (super-loop) programming for embedded systems. Discuss factors including determinism, memory footprint, development complexity, debugging difficulty, and provide guidelines for choosing each approach based on project requirements.' },
            { title: 'Wireless Protocol Selection for IoT', problemStatement: 'Analyze the trade-offs between BLE, Zigbee, LoRa, Wi-Fi, and Thread for a smart agriculture monitoring system. Consider range, power consumption, data rate, network topology, cost, and ecosystem maturity. Justify your recommended protocol choice.' },
            { title: 'Power Optimization Strategies', problemStatement: 'Describe a systematic approach to optimizing power consumption in a battery-powered wearable health monitor. Cover hardware techniques (voltage scaling, sleep modes, peripheral gating) and software techniques (event-driven architecture, clock management, efficient algorithms).' },
            { title: 'Embedded Security Challenges', problemStatement: 'Discuss the unique security challenges in IoT and embedded systems. Address secure boot, firmware update integrity, communication encryption on constrained devices, physical attack resistance, and the tension between security and resource constraints.' },
            { title: 'FPGA vs Microcontroller Comparison', problemStatement: 'Compare FPGAs and microcontrollers for real-time signal processing applications. Discuss latency, parallelism, development effort, power consumption, cost, and flexibility. Provide example applications where each technology is the better choice.' },
        ],
        modeling: [
            { title: 'Embedded System Architecture', problemStatement: 'Create a class diagram for an embedded sensor node showing Microcontroller, Sensor, Actuator, CommunicationInterface, PowerManager, and DataLogger classes with their relationships and key methods.', diagramType: 'ClassDiagram' },
            { title: 'Firmware Update Process', problemStatement: 'Model the over-the-air firmware update process as an activity diagram including version check, download, integrity verification, flash programming, validation, and rollback on failure with decision points at each stage.', diagramType: 'ActivityDiagram' },
            { title: 'RTOS Task Interaction', problemStatement: 'Create a class diagram showing RTOS components: Task, Scheduler, Mutex, Semaphore, Queue, Timer, and InterruptHandler with their dependencies and synchronization relationships.', diagramType: 'ClassDiagram' },
            { title: 'Sensor Data Pipeline', problemStatement: 'Model the sensor data acquisition pipeline as an activity diagram: ADC sampling, DMA transfer, digital filtering, calibration, threshold checking, data packaging, and wireless transmission with error handling branches.', diagramType: 'ActivityDiagram' },
            { title: 'IoT Device State Machine', problemStatement: 'Create a class diagram modeling IoT device states and transitions: Initialization, Active, LowPower, Sleep, FirmwareUpdate, Error, and Recovery with events triggering each transition.', diagramType: 'ClassDiagram' },
        ],
        quiz: [
            { title: 'Communication Protocols Quiz', questions: [
                { type: 'multiple-choice', title: 'I2C Bus', text: 'How many signal lines does the I2C bus require?', points: 5, singleChoice: true, answerOptions: [{ text: '2 (SDA and SCL)', isCorrect: true }, { text: '4 (SCLK, MOSI, MISO, CS)', isCorrect: false }, { text: '1 (data)', isCorrect: false }, { text: '3 (TX, RX, CLK)', isCorrect: false }] },
                { type: 'multiple-choice', title: 'SPI Communication', text: 'Which SPI signal selects the target slave device?', points: 5, singleChoice: true, answerOptions: [{ text: 'Chip Select (CS)', isCorrect: true }, { text: 'MOSI', isCorrect: false }, { text: 'SCLK', isCorrect: false }, { text: 'MISO', isCorrect: false }] },
            ]},
            { title: 'RTOS Concepts Quiz', questions: [
                { type: 'multiple-choice', title: 'Priority Inversion', text: 'Which mechanism solves the priority inversion problem in an RTOS?', points: 5, singleChoice: true, answerOptions: [{ text: 'Priority inheritance', isCorrect: true }, { text: 'Round-robin scheduling', isCorrect: false }, { text: 'Increasing stack size', isCorrect: false }, { text: 'Disabling interrupts', isCorrect: false }] },
                { type: 'multiple-choice', title: 'Schedulability', text: 'What is the utilization bound for Rate Monotonic Scheduling with a large number of tasks?', points: 5, singleChoice: true, answerOptions: [{ text: 'Approximately 69.3% (ln 2)', isCorrect: true }, { text: '100%', isCorrect: false }, { text: '50%', isCorrect: false }, { text: '80%', isCorrect: false }] },
            ]},
            { title: 'Embedded Hardware Quiz', questions: [
                { type: 'multiple-choice', title: 'ADC Resolution', text: 'What is the voltage resolution of a 12-bit ADC with a 3.3V reference?', points: 5, singleChoice: true, answerOptions: [{ text: 'Approximately 0.8 mV', isCorrect: true }, { text: 'Approximately 3.2 mV', isCorrect: false }, { text: 'Approximately 12.9 mV', isCorrect: false }, { text: 'Approximately 0.1 mV', isCorrect: false }] },
                { type: 'multiple-choice', title: 'Volatile Keyword', text: 'When must the volatile keyword be used in embedded C?', points: 5, singleChoice: true, answerOptions: [{ text: 'For variables modified by ISRs or mapped to hardware registers', isCorrect: true }, { text: 'For all global variables', isCorrect: false }, { text: 'For loop counters only', isCorrect: false }, { text: 'For constant values stored in Flash', isCorrect: false }] },
            ]},
        ],
        fileUpload: [
            { title: 'Embedded System Design Report', problemStatement: 'Upload a report documenting your embedded system design: hardware block diagram, peripheral selection rationale, firmware architecture, power budget analysis, communication protocol choices, and test results with oscilloscope or logic analyzer captures.' },
            { title: 'IoT Sensor Network Proposal', problemStatement: 'Upload a proposal for an IoT sensor network deployment. Include system architecture, sensor node hardware design, wireless protocol justification, cloud connectivity strategy, power management plan, estimated battery life calculations, and a bill of materials with cost analysis.' },
        ],
    },
    faqs: [
        { title: 'What is the difference between a microcontroller and a microprocessor?', answer: 'A microcontroller integrates CPU, memory (Flash, SRAM), and peripherals (GPIO, UART, ADC, timers) on a single chip, designed for embedded control tasks. A microprocessor contains only the CPU and requires external memory and I/O chips. Microcontrollers are self-contained; microprocessors need a surrounding system.' },
        { title: 'Why do we use the volatile keyword in embedded C?', answer: 'Volatile tells the compiler that a variable can change unexpectedly (by hardware or ISR) and must not be optimized away. Without volatile, the compiler might cache a register value in a CPU register and never re-read it, missing hardware updates. Always use volatile for memory-mapped peripheral registers and variables shared between ISR and main code.' },
        { title: 'What is the difference between I2C and SPI?', answer: 'I2C uses 2 wires (SDA, SCL) with addressing for multi-device support, but is slower (100 kHz-1 MHz). SPI uses 4+ wires (SCLK, MOSI, MISO, CS per device), is faster (up to 100 MHz), and full-duplex. I2C is simpler for many slow devices; SPI is better for high-speed data transfer to fewer devices.' },
        { title: 'How do I choose between an RTOS and bare-metal programming?', answer: 'Use bare-metal (super-loop) for simple applications with few tasks and relaxed timing. Use an RTOS when you need multiple concurrent tasks with different priorities, deterministic scheduling, or inter-task communication. RTOS adds memory overhead (typically 5-15 KB) but simplifies complex application logic and improves maintainability.' },
        { title: 'What is interrupt latency and how can I minimize it?', answer: 'Interrupt latency is the time from interrupt assertion to the first ISR instruction. Sources: current instruction completion, context save, vector fetch, and pipeline flush. Minimize by keeping ISRs short, avoiding long critical sections, using appropriate priority levels, and enabling tail-chaining on Cortex-M (NVIC handles back-to-back interrupts without full context save/restore).' },
        { title: 'How does DMA improve embedded system performance?', answer: 'DMA (Direct Memory Access) transfers data between peripherals and memory without CPU intervention. The CPU configures the DMA channel (source, destination, count, trigger) and is interrupted on completion. This frees the CPU for computation while data transfers happen in parallel. Essential for high-throughput applications: ADC sampling, SPI displays, and audio streaming.' },
        { title: 'What is a watchdog timer and why is it important?', answer: 'A watchdog timer is a hardware countdown that resets the system if not periodically refreshed ("kicked"). It recovers from firmware hangs, infinite loops, and deadlocks. The application must kick the watchdog regularly; failure to do so triggers a reset. Window watchdog variants require kicking within a specific time window, catching both too-slow and too-fast refresh patterns.' },
        { title: 'How do I calculate the required ADC sampling rate?', answer: 'The Nyquist theorem requires sampling at least twice the highest frequency component in the signal: f_sample >= 2 * f_max. In practice, use 5-10x oversampling for better reconstruction. Anti-aliasing low-pass filters (analog, before the ADC) must attenuate frequencies above f_sample/2. For a 1 kHz signal, sample at 5-10 kHz minimum.' },
        { title: 'What is the difference between FreeRTOS queues and semaphores?', answer: 'Queues transfer data between tasks (typed messages with FIFO ordering, blocking on full/empty). Semaphores synchronize task execution without data transfer. Binary semaphores signal events (ISR to task). Counting semaphores manage resource pools. Mutexes are special semaphores with priority inheritance for mutual exclusion. Use queues when you need to pass data; semaphores when you only need to signal.' },
        { title: 'How does BLE differ from classic Bluetooth?', answer: 'BLE (Bluetooth Low Energy) is designed for intermittent, low-data-rate communication with minimal power. Classic Bluetooth provides continuous, higher-bandwidth streaming (audio, file transfer). BLE uses GATT profiles with services and characteristics. Classic Bluetooth uses SPP, A2DP, HFP profiles. BLE devices can run for years on a coin cell battery; classic Bluetooth cannot.' },
        { title: 'What is a linker script and why do I need one?', answer: 'A linker script defines the memory layout for firmware: Flash start address and size, SRAM start and size, and section placement (.text in Flash, .data copied from Flash to SRAM, .bss in SRAM, .stack at the top of SRAM). It also defines the entry point and interrupt vector table location. Without it, the linker cannot place code and data correctly for the target MCU.' },
        { title: 'How do I debounce a mechanical switch in firmware?', answer: 'Mechanical switches bounce for 5-20 ms, generating multiple transitions. Software debouncing: sample the input periodically (e.g., every 10 ms via timer interrupt) and require N consecutive consistent readings before accepting a state change. Alternatively, use a state machine that ignores transitions for a lockout period after the first edge. Hardware debouncing uses RC filters or Schmitt triggers.' },
        { title: 'What is the difference between a hard real-time and soft real-time system?', answer: 'Hard real-time systems have strict deadlines where missing one causes system failure (airbag deployment, anti-lock braking). Soft real-time systems have deadlines where occasional misses degrade quality but do not cause failure (video streaming, UI responsiveness). Hard real-time requires deterministic scheduling, worst-case execution time analysis, and verified interrupt latency bounds.' },
        { title: 'How do FPGAs differ from microcontrollers for embedded applications?', answer: 'FPGAs implement custom digital logic in reconfigurable hardware fabric, providing massive parallelism and deterministic timing. Microcontrollers execute sequential software instructions. FPGAs excel at parallel signal processing, custom protocol implementations, and sub-microsecond latency. MCUs are easier to program, cheaper, and lower power for most control tasks. SoC FPGAs combine both with ARM cores plus FPGA fabric.' },
        { title: 'What is JTAG and how is it used for debugging?', answer: 'JTAG (Joint Test Action Group, IEEE 1149.1) provides on-chip debug access via a 4-5 wire interface (TCK, TMS, TDI, TDO, optional TRST). It enables setting breakpoints, single-stepping, reading/writing registers and memory, and flash programming. SWD (Serial Wire Debug) is a 2-wire ARM alternative offering the same functionality with fewer pins. Debug probes (J-Link, ST-Link) connect JTAG/SWD to the host PC.' },
        { title: 'How do I reduce power consumption in a battery-powered design?', answer: 'Systematically: (1) use the lowest clock frequency that meets timing requirements, (2) enter sleep modes whenever idle, (3) disable unused peripherals and clock domains, (4) reduce supply voltage if possible, (5) use event-driven wake instead of polling, (6) batch communication to minimize radio-on time, (7) optimize algorithms to reduce active time. Measure with a power analyzer to find and eliminate hidden consumers.' },
        { title: 'What is the purpose of the device tree in embedded Linux?', answer: 'The device tree is a data structure describing hardware topology that the kernel parses at boot. It replaces hard-coded board files with a portable description of peripherals, memory maps, interrupt connections, and clock relationships. Each node describes a device with compatible string, register addresses, and configuration properties. Device tree overlays enable runtime hardware description changes.' },
        { title: 'How does LoRa achieve long range with low power?', answer: 'LoRa uses chirp spread spectrum (CSS) modulation that spreads the signal below the noise floor. The receiver can demodulate signals 20 dB below noise. Higher spreading factors (SF7-SF12) increase range but reduce data rate (from 11 kbps to 0.3 kbps). Sub-GHz frequencies (868 MHz EU, 915 MHz US) have better propagation than 2.4 GHz. Low duty cycles (1% in EU) and short payloads minimize transmit energy.' },
        { title: 'What is the difference between polling and interrupt-driven I/O?', answer: 'Polling repeatedly checks a peripheral status flag in a loop, wasting CPU cycles when nothing is happening. Interrupt-driven I/O lets the CPU do other work (or sleep) and responds only when the peripheral signals an event. Interrupts are more efficient and responsive but add complexity (ISR design, shared variable protection, priority management). DMA further offloads the CPU by handling data transfers in hardware.' },
        { title: 'How do I choose the right communication protocol for my embedded system?', answer: 'Consider data rate, distance, power budget, number of devices, and topology. On-board between chips: SPI (fast, few devices), I2C (slower, many devices on 2 wires). Short range wireless: BLE (phone connectivity, low power), Wi-Fi (high bandwidth, higher power). Long range: LoRa (km range, very low data rate), cellular (NB-IoT, LTE-M). Industrial: CAN (robust, deterministic), RS-485 (long distance, differential).' },
    ],
    exerciseMessages: [
        'I am stuck on the UART driver exercise. My baud rate calculation gives a 3% error at 115200 baud. Is that acceptable?',
        'Has anyone gotten the I2C temperature sensor to respond? I keep getting NACKs on the address byte.',
        'My SPI flash driver reads all 0xFF. I think the chip might not be waking up from power-down mode properly.',
        'The FreeRTOS task crashes when I increase the sensor polling rate. I suspect a stack overflow. How do I check?',
        'My PID motor controller oscillates wildly. I set Kp=1.0 but the output overshoots by 200%. Any tuning advice?',
        'Does anyone know the correct clock configuration for 72 MHz on the STM32F103? My UART timing is way off.',
        'I cannot get DMA to work with the ADC. The transfer complete interrupt never fires. Is there a channel conflict?',
        'The BLE exercise says to use notifications but my phone app does not receive them. Do I need to enable the CCCD?',
        'My interrupt handler for the button fires multiple times per press. How long should the debounce delay be?',
        'I am confused about the linker script. Where exactly should the interrupt vector table be placed in Flash?',
        'My watchdog timer resets the system every 2 seconds even though I am kicking it. Is the prescaler set correctly?',
        'For the bootloader exercise, how do I jump from the bootloader to the application firmware? Do I need to relocate the vector table?',
        'The ADC readings are very noisy. Should I use hardware averaging or implement a software filter?',
        'My CAN bus messages are not being received by the other node. The bit timing configuration seems wrong for 500 kbps.',
        'I implemented the PWM but the motor does not start below 30% duty cycle. Is this a dead zone issue with the H-bridge?',
        'Can someone explain how to calculate the pull-up resistor value for I2C? The datasheet says the rise time is too slow.',
        'My FreeRTOS mutex causes a deadlock when two tasks try to acquire it. How do I implement proper lock ordering?',
        'The SPI clock polarity seems wrong for my sensor. How do I determine the correct CPOL and CPHA settings from the datasheet?',
        'I am getting a Hard Fault exception when I try to access a peripheral register. Is the clock for that peripheral enabled?',
        'The power consumption in sleep mode is 500 uA instead of the expected 2 uA. What could be drawing the extra current?',
        'My timer interrupt fires at the wrong frequency. I think I miscalculated the prescaler and auto-reload values.',
        'How do I share data between an ISR and a task in FreeRTOS? Using a global variable with volatile seems unreliable.',
        'The ESP32 web server crashes after about 100 requests. I think I am leaking memory in the request handler.',
        'My LoRa node only achieves 2 km range instead of the expected 10 km. Should I increase the spreading factor?',
        'I connected the logic analyzer and the I2C waveform looks correct but the sensor still NACKs. Could it be a 3.3V vs 5V level issue?',
        'The SD card initialization fails on the first power-up but works after a reset. Is there a required initialization delay?',
        'My embedded Linux device takes 45 seconds to boot. How do I profile the boot process and reduce boot time?',
        'The FPGA synthesis reports timing violations on the SPI slave interface. Should I add pipeline registers?',
        'For the alarm clock exercise, how do I keep the RTC running during standby mode? The time resets on wake.',
        'My FIR filter implementation works but is too slow for real-time audio at 44.1 kHz. Can I use the hardware MAC instructions?',
        'I am having trouble with the keypad scanner. Ghosting occurs when I press two keys in the same row simultaneously.',
        'The BLE connection drops after exactly 30 seconds. I think the supervision timeout is too short. How do I change it?',
        'My device tree overlay for the SPI device is not loading. The kernel log says "OF: overlay: ERROR: fragment target property not found". What does that mean?',
        'Can someone help with the DMA circular buffer for ADC? The half-transfer and transfer-complete interrupts both fire at the same time.',
        'My PWM frequency is 20 kHz but I can hear a high-pitched whine from the motor. Should I go higher?',
        'The flash memory write takes 10 ms per page but my application cannot tolerate that latency. Can I write in the background?',
        'My bare-metal code works fine but breaks when I add FreeRTOS. The systick interrupt seems to conflict with my existing timer.',
        'I tested the sleep mode current and it increases by 100 uA when I enable the RTC. Is that normal for this MCU?',
        'The encoder readings jump erratically at high RPM. I think I am missing counts. Should I use the timer capture mode instead of GPIO interrupts?',
        'My UART receives garbage characters from the GPS module. I tried 9600 and 115200 baud but neither works. Could it be 4800?',
        'I finished the bootloader but the CRC check fails on valid firmware images. I think my CRC-32 polynomial is wrong.',
        'The I2C bus hangs after a communication error and the SDA line stays low. How do I implement bus recovery?',
        'For the web server exercise, is it better to serve a static HTML page or generate it dynamically with sensor values?',
        'My PID controller has steady-state error even with the integral term. The integral windup clamps seem too aggressive.',
        'The Zigbee mesh network loses packets when nodes are more than 3 hops apart. Is there a maximum hop count?',
        'I am using 16-bit fixed-point Q15 format for the DSP exercise but the filter output clips. Should I switch to Q31?',
        'My sensor calibration drifts over temperature. Should I store calibration coefficients for multiple temperature points?',
        'The oscilloscope shows ringing on the SPI clock signal. Should I add series termination resistors?',
        'I just realized my firmware reads the wrong ADC channel because the channel mapping on this MCU is not sequential. Check the datasheet!',
        'Successfully got OTA firmware updates working over BLE. The key was splitting the image into 20-byte MTU chunks and adding sequence numbers.',
    ],
    channelMessages: [
        'Can someone explain the difference between a timer in PWM mode and output compare mode? When should I use each?',
        'I am confused about the ARM Cortex-M exception model. What is the difference between interrupts and exceptions?',
        'How do I calculate the maximum number of I2C devices I can put on a single bus given the capacitance limit?',
        'What are the trade-offs between using DMA and interrupt-driven I/O for UART communication?',
        'Can someone recommend a good logic analyzer for debugging SPI and I2C at home? Under 50 euros if possible.',
        'How does the FreeRTOS tick rate affect system performance and power consumption?',
        'I am trying to understand the difference between NOR flash and NAND flash for embedded storage. Which should I use?',
        'What is the practical difference between SWD and JTAG for debugging ARM microcontrollers?',
        'How do I estimate battery life for a BLE sensor node that wakes up every 10 seconds?',
        'Can someone explain what happens during a Hard Fault on Cortex-M and how to debug it?',
        'What is the advantage of using a hardware CRC peripheral over a software CRC implementation?',
        'How do I choose between using a bare-metal delay loop and a hardware timer for precise timing?',
        'What is the difference between edge-triggered and level-triggered interrupts, and when should I use each?',
        'Can someone explain the concept of memory-mapped I/O and how it works on ARM microcontrollers?',
        'What are the benefits of using Zephyr RTOS over FreeRTOS for a new IoT project?',
    ],
};
